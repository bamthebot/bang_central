version: '3.7'
services:
  web:
    image: twitchbambot/bang_central:master
    volumes:
     - .:/home/bam/bangcentral
    environment:
     - CLIENT_ID
     - CLIENT_SECRET
     - ENVIRONMENT
     - DJANGO_SECRET_KEY
     - ALLOWED_HOST
     - DB_USER
     - DB_PASSWORD
     - DB_HOST
     - DB_PORT
    networks:  # <-- here
      - nginx_network
  nginx:
    image: nginx:1.13
    ports:
      - $HOST_HTTP_PORT:80
    volumes:
      - ./config/nginx/conf.d:/etc/nginx/conf.d
    depends_on: 
      - web
    networks:  # <-- here
      - nginx_network
  watchtower:
    image: containrrr/watchtower
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /root/.docker/config.json:/config.json
    command: --shedule "0 4 1 * *" --cleanup
networks:  # <-- and here
  nginx_network:
    driver: bridge
