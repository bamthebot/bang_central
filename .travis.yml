language: python
environement:
  - ENVIRONMENT=test

services:
  - docker
  - docker-compose

before_install:
  - docker build -t bang_central .

script:
  - docker-compose run web python manage.py test

